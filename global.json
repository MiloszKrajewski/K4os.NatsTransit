{
  "sdk": {
    "version": "8.0.101",
    "rollForward": "latestMinor"
  },
  "scripts": {
    "clean": ".\\nuke clean",
    "restore": ".\\nuke restore",
    "build": ".\\nuke build",
    "rebuild": ".\\nuke rebuild",
    "release": ".\\nuke release",
    "test": ".\\nuke test",
    "services": "cd docker && docker compose up -d --remove-orphans",
    "frontend": "dotnet run --project src/FlowDemo.Api --configuration Release",
    "backend": "dotnet run --project src/FlowDemo.Backend --configuration Release"
  }
}